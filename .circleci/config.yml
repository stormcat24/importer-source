version: 2.1

# comment out 1
jobs:
  build:
    working_directory: /go/src/github.com/stormcat24/importer-source
    # comment out 2
    docker:
      - image: circleci/golang:1.17
    steps:
      - checkout
      # comment out 3
      - restore_cache:
          keys:
            - go-mod-v4-{{ checksum "go.sum" }}
      - run:
          name: Install Dependencies
          command: go mod download
      - save_cache:
          key: go-mod-v4-{{ checksum "go.sum" }}
          paths:
            - "/go/pkg/mod"
      - run:
          name: Run tests
          command: |
            mkdir -p /tmp/test-reports
            # comment out 4
            # gotestsum --junitfile /tmp/test-reports/unit-tests.xml
      - store_test_results:
          path: /tmp/test-reports

workflows:
  sample:
    jobs:
      - build
